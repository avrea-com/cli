.TH AVR\-VM\-RESUME 1 "" "avr 0.2.0" "Avrea CLI"
.SH NAME
avr-vm-resume \- Resume a PAUSED VM from its preserved snapshot.
.SH SYNOPSIS
.B avr vm resume
[OPTIONS] VM_ID
.SH DESCRIPTION
Resume a PAUSED VM from its preserved snapshot.
.PP
The existing password remains valid. If a memory restore cannot complete, retry with --discard-memory to boot the preserved disk without restoring running processes.
.SH ARGUMENTS
.TP
\fIVM_ID\fR
\&
.SH OPTIONS
.TP
\fB\-\-org\fR \fITEXT\fR
Organization ID. Uses default org if not specified (see: avr config set org).
.TP
\fB\-\-discard\-memory\fR
Ignore saved memory state and fresh-boot the preserved disk (recovery for a stuck memory restore).
.TP
\fB\-\-wait\fR
Wait until the VM is RUNNING and connectable.
.TP
\fB\-\-wait\-timeout\fR \fIINTEGER\fR
Seconds to wait when --wait is set. (default: 300)
.TP
\fB\-\-json\fR
Emit the raw API response as JSON.
.SH SEE ALSO
.BR avr (1),
.BR avr-vm (1),
.BR avr-vm-bootstrap (1),
.BR avr-vm-create (1),
.BR avr-vm-delete (1),
.BR avr-vm-list (1),
.BR avr-vm-pause (1),
.BR avr-vm-port-forward (1),
.BR avr-vm-rdp (1),
.BR avr-vm-show (1),
.BR avr-vm-ssh (1),
.BR avr-vm-ssh-config (1),
.BR avr-vm-start (1),
.BR avr-vm-stop (1),
.BR avr-vm-update (1),
.BR avr-vm-usage (1),
.BR avr-vm-vnc (1)
